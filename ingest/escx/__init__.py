"""ESCX ingest — стартовый набор для сбора данных о конфликтах.

Принцип: нулевые зависимости. Только стандартная библиотека Python 3.11+.
Хранилище — SQLite (для 60 диад этого хватает с большим запасом).
Путь роста: те же таблицы переносятся в Postgres без изменения схемы.
"""
__version__ = "0.1.0"
